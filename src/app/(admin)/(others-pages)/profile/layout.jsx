import React from "react";

export const metadata = {
  title: "Afghan Safar | Admin Profile",
  description:
    "This is the admin profile page for Afghan Safar, providing admin users with an overview of their account details and settings within the admin dashboard.",
};
const layout = ({ children }) => {
  return children;
};

export default layout;
